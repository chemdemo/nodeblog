<% include include/html-header %>
<link rel="stylesheet" href="/style/user.css" />

	<div class="page-body clearfix">
		<form class="user-signup" action="/signup" method="POST">
			<h2>注册</h2>
			<p>
				<label for="name">用户名<b class="required">（*）</b>：</label>
				<input class="input" type="name" name="name" id="name" placeholder="Input your name" value="<%= (locals.user ? locals.user.name : '') %>" />
			</p>
			<p>
				<label for="email">邮箱<b class="required">（*）</b>：</label>
				<input class="input" type="email" name="email" id="email" placeholder="Input your email" value="<%= (locals.user ? locals.user.email : '') %>" />
			</p>
			<p>
				<label for="pass">密码<b class="required">（*）</b>：</label>
				<input class="input" type="password" name="pass" id="pass" placeholder="Input your password" />
			</p>
			<p>
				<label for="site">网站：</label>
				<input class="input" name="site" id="site" placeholder="Input your site" value="<%= (locals.user ? locals.user.site : '') %>" />
			</p>
			<button type="submit" class="i-btn i-btn-blue">
				<i class="icon-checkmark"></i>提交
			</button>
		</form>

		<% if(locals.error) { %>
		<p class="error"><strong><%= locals.error %></strong></p>
		<% } %>
	</div>

<script data-main="/js/user.js" src="/js/libs/requirejs/require.js"></script>
</body>
</html>